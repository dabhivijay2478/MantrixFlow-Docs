---
title: Connections
description: Create and test PostgreSQL, HubSpot, and Stripe connections in the MantrixFlow workspace.
---

Open **Connections** to create reusable database and SaaS credentials.

## Create a connection

1. Click **+ New Connection**.
2. Choose the role: Source or Destination.
3. Choose **PostgreSQL**, **HubSpot**, or **Stripe** for a source. Choose
   **PostgreSQL** for a destination.
4. Fill the connection fields.
5. Click **Test Connection**.
6. Save after the test succeeds.

## Source connections

PostgreSQL source users should have read access to the schemas and tables you
plan to include. HubSpot sources use a read-only private app access token with
the scopes required by the selected streams. See the [HubSpot source
guide](/connections/sources/crm/hubspot). Stripe sources use a secret or
restricted read key. See the [Stripe source
guide](/connections/sources/finance/stripe).

## Destination connections

Destination users should have write access to pre-created destination tables. MantrixFlow validates those tables during pipeline runs.

## Good operating habits

- Re-test connections after credential rotation.
- Keep source users read-only where possible.
- Keep destination table ownership and permissions explicit.
