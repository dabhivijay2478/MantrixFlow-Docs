---
title: Connections Overview
description: Create and test the PostgreSQL source and destination connections used by the live MantrixFlow pipeline flow.
---

Connections are reusable credentials. The current public workflow documents PostgreSQL as both source and destination.

## Current live connectors

| Role | Connector | Status |
| --- | --- | --- |
| Source | PostgreSQL | Live |
| Destination | PostgreSQL | Live |

Other connector tiles may appear in internal code or future catalog work, but they are not documented here as available until their create, test, save, pipeline, and run path is complete in the app.

## Create a source connection

1. Open **Connections**.
2. Click **+ New Connection**.
3. Set the role to **Source**.
4. Choose **PostgreSQL**.
5. Fill the host, port, database, username, password, schema, and SSL mode.
6. Click **Test Connection**.
7. Save after the test succeeds.

## Create a destination connection

1. Open **Connections**.
2. Click **+ New Connection**.
3. Set the role to **Destination**.
4. Choose **PostgreSQL**.
5. Fill the destination database credentials.
6. Click **Test Connection**.
7. Save after the test succeeds.

Destination users need write access to pre-created destination tables.

## Network access

If your database is hosted behind a provider firewall, allow traffic from the MantrixFlow runtime before testing the connection. See [Private database access](/connections/private-database-access).
